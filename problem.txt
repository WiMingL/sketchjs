1. getImageData 取的像素值有误问题
  当获取的区域比较小的时候，获取的像数值是比较准确的，但一旦获取的区域太大，就会导致获取的像数值与设置的值不相同
如设置的元素填充色为 rgb(2,0,0,) 在取区域比较小的情况下 ctx.getImageData(x,y,1,1)取出的值是匹配的，但如果取ctx.getImageData(x,y,100,100) 那么会出现其他的像素值，以及不同透明度的值

鉴于这种情况，我们对获取大区域的范围的元素进行分区获取，分成若干个较小区域来获取对应的元素像素值
